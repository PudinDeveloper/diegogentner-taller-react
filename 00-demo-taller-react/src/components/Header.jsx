const Header = ({title}) => (
        <h1>{title}</h1>
)

export default Header;


// const Header = (props) => {
//     const { title, name, age } = props;

//     return (
//         <h1>{title}</h1>
//     )
// }